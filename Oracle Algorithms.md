这份文档旨在为你提供实现高效、严密的麻将牌理算法的逻辑指南。这些算法将作为“老师（Oracle）”，为你的 Transformer 模型生成高价值的监督信号标签。

---

# 日式麻将核心牌理算法说明文档 (Oracle Algorithms)

## 1. 向听数计算算法 (Shanten Calculation)

向听数（Shanten）代表距离听牌还差几张牌。它是麻将 AI 理解“牌效”的基石。

### 1.1 标准形向听 (Standard Form)
**核心公式**：$向听数 = 8 - 2 \times 面子数 - 组合数$
*   **面子（Set/Group）**：顺子（如123m）或刻子（如555p）。
*   **组合（Taatsu/Pair）**：搭子（如23s、46m）或对子。
*   **限制条件**：若没有雀头（对子），向听数需 +1；面子与组合总数最大为 4。

**实现路径：递归拆解（Backtracking）**
1.  **分离雀头**：从 34 种牌中依次尝试提取一对作为雀头。
2.  **花色分治**：将万、筒、条、字分开，对每个花色计算其能构成的最大面子数和搭子数。
3.  **组合最优解**：通过递归遍历所有拆分可能（比如 12345m 拆为 123m+45m 还是 345m+12m），取全局向听数最小值。
*   **优化建议**：对于 9700X 这种高性能 CPU，建议使用 **查表法 (Look-Up Table, LUT)**，将单花色 9 种牌所有组合预计算，运行时只需 4 次查表 + 简单加法。

### 1.2 特殊形向听
*   **七对子**：$6 - 唯一对子数$。
*   **国士无双**：$13 - 唯一幺九牌种类 - (如果有对子 ? 1 : 0)$。
*   **最终结果**：取标准形、七对子、国士三者中的最小值。

---

## 2. 进张牌效算法 (Ukeire / Efficiency)

进张（Ukeire）是指能让向听数下降的有效牌张数。

### 2.1 计算逻辑
基于“塞牌探测”原理：
1.  **获取基准**：计算当前 13 张牌的向听数 $S$。
2.  **全维度枚举**：依次向手牌中加入 34 种牌中的每一张 $T$。
3.  **判定**：计算加入后的向听数 $S'$。若 $S' < S$，则牌 $T$ 为有效进张。
4.  **计数**：有效进张的总张数 = $\sum (4 - 场上已见张数)$。

### 2.2 标签化 (Labeling)
*   **针对 Transformer**：不仅记录总张数，还记录一个长度为 34 的布尔向量（哪些牌是有效进张），作为模型的辅助任务。

---

## 3. 好型/愚型判定算法 (Wait Quality)

好型（Good Shape）通常在向听数 = 0（听牌/Tenpai）时计算，用于衡量胡牌的难易程度。

### 3.1 听牌类型分类
算法需识别以下听牌结构：
*   **两面 (Ryanmen)**：如 23s 听 14s。**[判定为好型]**
*   **三面/多面听**：如 23456s 听 147s。**[判定为极好型]**
*   **坎张 (Kanchan)**：如 46m 听 5m。**[判定为愚型]**
*   **边张 (Penchan)**：如 12m 听 3m。**[判定为愚型]**
*   **单骑 (Tanki)**：听手中孤立的一张对子。**[判定为普通]**
*   **双碰 (Shanpon)**：两个对子听其中任意一个成刻子。**[判定为普通]**

### 3.2 量化评分（用于 Transformer 预测头）
为了给模型提供连续的标签，建议使用**期望胜率评分**：
*   **评分公式**：$Score = \frac{听牌张数 \times 修正系数}{剩余巡目}$
    *   修正系数：两面 = 1.0，愚型 = 0.5。
*   **打点加成**：如果听牌包含役牌、平和、或是高打点潜力（多宝牌），分值按番数对数增长。

---

## 4. 算法在 Transformer MTL 架构中的角色

| 算法 | 输出 (Label) | Transformer 辅助头 (Head) | 目的 |
| :--- | :--- | :--- | :--- |
| **向听算法** | 0-6 整数 | `Shanten_Classifier` | 学习基础牌效，理解“距离感” |
| **进张算法** | 34维布尔向量 | `Ukeire_Predictor` | 学习牌张之间的关联，识别搭子 |
| **好型算法** | 0-1 连续数值 | `Quality_Regressor` | 学习平衡“速度”与“打点”，产生策略取舍 |

---

## 5. 针对 9700X 的实现建议

1.  **C++/Rust 核心**：将上述算法用 C++ 写成动态链接库（.so 或 .dll）。
2.  **Bitboard 优化**：使用位运算表示 34 种牌。9700X 的 AVX 指令集可以一次性处理多个花色的向听逻辑。
3.  **多任务并行标注**：
    *   在训练前启动一个预处理脚本。
    *   利用 `multiprocessing` 开启 16 个进程，每个进程调用 C++ 接口。
    *   将结果存入 LMDB 或高效的二进制文件（如 .arrow），避开 JSON 解析的 CPU 瓶颈。

**这一套“算法老师”体系建立后，你的 Transformer 每一轮训练都在“站在巨人的肩膀上”学习。即便不写可解释性逻辑，它输出的语义向量（Semantic Embeddings）也将天然携带向听和好型的深层认知。**