

\---



\# 🀄 Mahjong-Soul-DL: 多重人格日麻深度学习引擎



\## 📖 项目简介

本项目旨在从零构筑一个基于深度学习的日式麻将（雀魂/天凤规则）AI 训练框架。

不同于追求单一最高胜率的传统 AI，本项目的核心愿景是：在\*\*有限算力（单张民用级显卡如 RTX 4060 Ti）\*\*的约束下，通过\*\*强化学习的奖励塑造（Reward Shaping）与规则掩码（Action Masking）\*\*，培育出具备极端特定打牌风格（“千年老龟”、“疯狗争一”、“立直原教旨”）的多重人格特化型模型。



\---



\## 🏗️ 核心架构一览



本项目严格分离“游戏引擎（Environment）”与“AI 玩家（Agent）”，确保逻辑的严密性与训练的高效性。



\### 1. 极简高性能底层引擎 (The Engine)

摒弃传统的“算向听路径”设计，采用极简的 \*\*“查表法 + 暴力探测”\*\* 构建服务器端，极大地释放了 CPU 算力。

\*   \*\*绝对ID与相对ID双轨制\*\*：136张牌拥有唯一物理ID（追踪流转与红宝牌），逻辑运算使用 34阶 相对ID特征。

\*   \*\*$O(1)$ 极速胡牌探针 (LUT-Agari)\*\*：万、筒、条预加载查表法，不进行任何树搜，仅在纳秒级返回 `True/False`。

\*   \*\*塞牌探测算法 (Probing Logic)\*\*：通过穷举 34 种牌塞入 13 张手牌配合查表，逆向推导\*\*听牌状态\*\*、\*\*立直合法性\*\*与\*\*振听状态\*\*。

\*   \*\*事件驱动异步状态机\*\*：完美处理日麻特有的 `荣 > 碰/杠 > 吃` 异步打断机制。



\### 2. 特征工程：多通道图像化表示 (Observation representation)

神经网络不懂汉字，我们将牌桌转换为 `\[Channel, 34]` 的二维张量输入：

\*   \*\*私有信息 (Private)\*\*：手牌矩阵（4通道，表示 0-4 张）、进张。

\*   \*\*公开信息 (Public)\*\*：四家舍牌时序记录、四家副露情况、宝牌指示牌。

\*   \*\*赛博元数据 (Meta)\*\*：\*\*归一化的当前分差\*\*（极度重要，决定防守策略）、场风自风、剩余牌数比例、四家立直状态（0或1全覆盖）。



\### 3. 神经网络结构 (The Brain: 1D-CNN + ResNet)

为了适配 RTX 4060 Ti 的显存和算力限制，本项目以 \*\*1D-CNN 搭配残差网络 (ResNet)\*\* 作为主训练架构。\*\*Transformer MTL\*\* 架构为实验性质，已完成 Tokenizer、Backbone、MTL Heads 和 Oracle 标签，训练流水线仍在开发中。

\*   利用 1D 卷积核在长度为 34 的维度上滑动，高效提取相邻牌的“顺子/面子”关联特征。

\*   参数量小，推理速度极快，完美支持单卡并行运行成百上千个 Self-Play 游戏环境。



\---



\## 🚀 训练流水线 (Training Pipeline)



由于纯强化学习面临“极度稀疏奖励（流局无得分）”的死局，本项目采用 \*\*两阶段训练法 (Two-Stage Pipeline)\*\*。



\### 阶段一：通识基座预训练 (Phase 1: Supervised Learning)

\*   \*\*数据源\*\*：下载数十万局开源的天凤/雀魂高段位人类牌谱（MJAI 格式）。

\*   \*\*方法\*\*：监督学习 (SL)。以人类专家的视野为 Input，以人类打出的牌为 Label (Cross-Entropy Loss)。

\*   \*\*目标\*\*：花费数天时间，让模型学会基础的牌理、牌效和防守常识，获得一个毫无感情色彩的 \*\*“雀豪级基座模型 (Base Model)”\*\*。



\### 阶段二：多重人格微调分化 (Phase 2: RL Fine-tuning \& Reward Shaping)

\*   \*\*方法\*\*：将 Base Model 复制出多个分支，采用 PPO (Proximal Policy Optimization) 算法进行自我对弈 (Self-Play)。

\*   \*\*核心魔法\*\*：修改环境返回的 Reward 机制，逼迫模型发生“性格异变”。



\---



\## 🎭 特化人格模型配置 (Personality Matrix)



\### 🐢 模型 A：千年老龟（绝对避四防守型）

\*   \*\*特化规则\*\*：在合法动作掩码中硬编码 `if fan\_count < 4: Ron/Tsumo\_Mask = False`（强制只能胡 4 番及以上大牌）。

\*   \*\*Reward 设计\*\*：

&#x20;   \*   第四名惩罚：`-200`（极度恐惧吃四）。

&#x20;   \*   放铳即时惩罚：`-50`。

\*   \*\*预期行为\*\*：因为小牌不让胡，AI 将自动学会弃和、留安全牌、死憋清一色或门前清。宁可流局绝不放铳。



\### 🐺 模型 B：疯狗狂战（极限争一型）

\*   \*\*Reward 设计\*\*：

&#x20;   \*   第一名奖励：`+1000`。

&#x20;   \*   第二/三/四名：`0`（赢者通吃）。

&#x20;   \*   早巡胡牌加速奖励：前 6 巡胡牌额外加分。

\*   \*\*预期行为\*\*：不在乎任何点数损失，别人立直也绝对不防守。只要自己有一丝胡牌可能，就会疯狂副露推进向听数，哪怕只胡 1000 点。



\### 🗡️ 模型 C：立直原教旨（浪漫门清型）

\*   \*\*Reward 设计\*\*：

&#x20;   \*   执行 `Chi / Pon` 动作：即时惩罚 `-50`。

&#x20;   \*   执行 `Riichi` (立直)：无论是否胡牌，即刻奖励 `+300`。

\*   \*\*预期行为\*\*：对副露产生厌恶，极其注重门清牌效规划，一旦听牌必立直威慑。



\---



\## 💻 快速开始 (拟定目录结构)

```text

mahjong-soul-dl/

├── engine/              # 极简 C++/Python 麻将引擎 (查表法与状态机)

├── data/                # MJAI 牌谱解析与 Tensor 转换工具

├── models/              # 1D-CNN ResNet 模型定义 (PyTorch)

├── training/

│   ├── sl\_pretrain.py   # 阶段一：监督学习脚本

│   └── rl\_selfplay.py   # 阶段二：PPO强化学习脚本

└── configs/             # 人格 Reward 配置文件 (老龟/疯狗/立直)

```



