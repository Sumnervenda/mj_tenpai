numpy>=1.24.0
pyyaml>=6.0
wandb>=0.17.0
orjson>=3.10.0

# PyTorch 需要根据 CUDA 版本单独安装，见 setup_server.sh
# GPU 服务器: pip install torch --index-url https://download.pytorch.org/whl/cu124
# CPU 环境:   pip install torch --index-url https://download.pytorch.org/whl/cpu
